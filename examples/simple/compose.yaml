services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: example
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d example"]
      interval: 1s
      timeout: 5s
      retries: 10
