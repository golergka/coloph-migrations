[project]
name = "simple"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "coloph-migrations",
]

[tool.uv.sources]
coloph-migrations = { path = "../../", editable = true }
