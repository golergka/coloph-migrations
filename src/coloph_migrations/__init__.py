"""Implementation package for the coloph-migrate CLI.

Only the command-line interface is a stable public contract.
"""

from importlib.metadata import version


__version__ = version("coloph-migrations")
